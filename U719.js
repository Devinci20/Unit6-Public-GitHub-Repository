alert("Hello");
var alert = 2;
alert("World");

/** In this script, the first alert has no variable and since the second alert does it doesn't do anything.
 Now if I were to get rid of the variable then the script would alert, "Hello" in the first alert then "World" in the second.**/